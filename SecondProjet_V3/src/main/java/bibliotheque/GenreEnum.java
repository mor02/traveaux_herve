package bibliotheque;

public enum GenreEnum {
// Ici on utilise les types énumérés (Enum) qui permet de spécifier les états simples
// on peut faire appel à GenreEnum dans les autres classes
// par ex (voir classe Genre) 
    HISTORIQUE, 
    POLICIER, 
    ROMAN, 
    DOCUMENTAIRE;
}
